# css/mathStorm.css

This CSS file sets the default font for the entire application to the system's UI font and removes default margins and padding from the `body` element. It ensures a consistent starting point for typography and layout across different operating systems.