class BlogPostContent {
  static title() {
    return 'The Space Between';
  }

  static subtitle() {
    return 'On optimized augmentation, human-in-the-loop tools, and the part of AI nobody should squeeze out';
  }

  static byline() {
    return 'recursi.dev · April 2026 · written by our eponymous and totally real intern, Scratchy 🐾';
  }

  static intro1() {
    return `Most of the talk around large language models still falls into three buckets. There's the <strong>chatbot web interface</strong> everybody knows — type into a box, get a response, copy the useful parts into your real work. There's the <strong>API</strong> world, with keys, billing, plumbing, and a more developer-shaped relationship to the model. And then there's the <strong>autonomous agent</strong> idea: give it a goal, let it run, and hope it doesn't go wandering into a ditch.`;
  }

  static intro2() {
    return `All of that is real, and all of it has its place. But the part I care about most is the middle — the stretch where the human stays present, the tooling gets smarter around them, and the result is a lot more capable than raw chat without turning into full automation.`;
  }

  static middle1() {
    return `That's the territory I mean by <strong>optimized augmentation</strong>. When the loop gets tighter — when you can keep your place, keep your context, and keep moving — you get something that feels genuinely different from either plain chat or full autonomy. The interface gets better. The output holds together better. You can actually see what's going on.`;
  }

  static middle2() {
    return `This is where a lot of the interesting tools are showing up: visual coding environments, structured prompt builders, browser companions, document tools that give chatbot output somewhere to live instead of letting it rot in scrollback, local tools for kids, music systems, drawing systems, interactive notebooks, and all the odd hybrids that don't fit neatly into anybody's approved product menu.`;
  }

  static middle3() {
    return `These aren't agents in costume. They're <em>augmentation tools</em> — things that make AI easier to work with, easier to learn from, and more creatively useful while keeping the person in the loop where they belong.`;
  }

  static why1() {
    return `This matters because the chatbot interface, good as it is, still leaves some obvious holes. It's not a full working environment. It's not a great place to organize a body of work, preserve structured output, build visual systems, or keep refining something across sessions without a bunch of awkward manual glue.`;
  }

  static why2() {
    return `The API solves some of that, sure, but it's a different universe. It assumes billing, credentials, plumbing, and a developer mindset. For plenty of people that's fine. But for kids, musicians, teachers, artists, and curious non-coders — people who learn by playing with tools — that's not just a different workflow. It's a different conversation.`;
  }

  static why3() {
    return `Agents solve a different problem too. Agents are for when you want the system to go off and handle something. The middle ground is for when you want to stay involved — stay hands-on, stay curious, stay fast — and still get a lot more leverage than a plain chat window gives you.`;
  }

  static lines1() {
    return `Here's where things get a little tricky. AI companies are starting to draw sharper boundaries between consumer chat products, official APIs, and everything else. Type into the website and you're a user. Call the API and you're a developer. Build tooling around the web interface — even with a human sitting right there — and suddenly you can get shoved into a category that was really meant for something else.`;
  }

  static lines2() {
    return `To be fair, some of that is understandable. If somebody is running an effectively headless loop against a consumer product and treating it like discounted infrastructure, companies are obviously going to care. Cost matters. Abuse matters. Product boundaries matter.`;
  }

  static lines3() {
    return `But a structured prompt builder is not an autonomous loop. A Markdown tool that turns chatbot output into something durable is not API abuse. A local Scratch helper for kids is not a stealth backend. A browser overlay that makes a conversation easier to work with is not the same thing as replacing the human. Not even close.`;
  }

  static lines4() {
    return `The important distinction, at least to me, is pretty simple: <em>human-in-the-loop augmentation</em> is a real category. It's different from autonomous access, different from raw API use, and different from plain chat. If companies are going to draw lines, they should at least see that territory clearly instead of flattening it into "everything else."`;
  }

  static recursi1() {
    return `<strong>Recursi</strong> is one example of what this looks like when you actually build in that space. It's a very tuned environment for building with AI — a fast collaboration loop where you stay present, you steer the architecture, and the AI does a lot of the heavy lifting inside a structure you control.`;
  }

  static recursi2() {
    return `It has two sides. <strong>Vibes</strong> is the structured one — visual, project-focused, and especially good at building dynamic web pages out of small composable functions. That matters because language models do a lot better when they can replace or reorganize clean units instead of flailing around inside giant blobs of HTML. <strong>YOLO</strong> is the unrestricted side — local, executable, self-modifying, full filesystem access — for when you really do need that. They're not separate products so much as two sides of one evolving system, and more and more they help build and improve each other.`;
  }

  static recursi3() {
    return `<strong>Markdown Notebook</strong> is a small but very clear example of why this space matters. Every chatbot user hits the same wall eventually: you get something good out of a conversation, and then... now what? The web UI usually isn't great at turning one good answer into something you can actually keep working on. Markdown Notebook fills that gap — paste in rich text from a chatbot or the web, clean it to Markdown, edit from either side, save a single HTML file you actually own, and come back later without losing the thread. It's a helper tool, but it makes a bigger point: the right augmentation layer can add real missing functionality without forcing everything through an API-shaped hole.`;
  }

  static recursi4() {
    return `<strong>Scratchy</strong> is the playful side — a local AI-powered coding environment for <a href="https://scratch.mit.edu" target="_blank">Scratch</a>, aimed at kids and anyone else who learns better through blocks and pictures than through terminals and dashboards. You start from a sample project or bring your own .sb3 file, describe what you want, and Scratchy makes surgical patches instead of regenerating the whole thing. Written by our eponymous and <em>totally real</em> intern, naturally. The joke is part of it, but the shape of the tool matters more: it's local, creative, kid-friendly, and very obviously the result of a human imagination working with AI instead of just tossing the problem to an agent.`;
  }

  static recursi5() {
    return `And then there's <strong>Aardvark</strong>, which started in 2005 as a little Firefox extension for poking at web pages and has since grown into something much bigger. It now includes a browser extension that can open YouTube videos in a cleaner player, build curated playlists, and support a pretty wild piano system with 3D visualization, synchronized piano rolls, MIDI input, instruments, looping, and more. Some of that is already visible. Some is still in beta. None of it is vapor. It's just another example of the same basic point: there's a lot of creative territory between a raw platform and a fully autonomous system, and that territory is worth exploring even when it doesn't fit neatly into somebody else's product taxonomy.`;
  }

  static bigger1() {
    return `That bigger territory is really the point. The space between "type in a chat box" and "deploy an agent" is a lot larger than people make it sound, and it's full of useful work: browser augmentation, visual workflows, kid-friendly tools, cross-model experimentation, structured prompt systems, local-first experiments, document-app hybrids, and other things that only seem unusual because the official categories are still so narrow.`;
  }

  static bigger2() {
    return `If the answer to all of that becomes "use the API or don't build it," a huge amount of innovation gets filtered out before it even starts — and not just for professionals. Especially for the people who learn by playing, remixing, dragging, dropping, composing, and iterating their way into skill. Kids are not going to meet this future through billing dashboards. They're going to meet it through tools.`;
  }

  static bigger3() {
    return `So no, this isn't an argument against APIs, and it isn't an argument against reasonable boundaries. It's an argument for taking this middle category seriously. Human-in-the-loop augmentation is not some loophole. It's one of the most interesting parts of the whole landscape.`;
  }

  static closing1() {
    return `Recursi is highly optimized, deeply visual, occasionally weird, and built through an unusually fast collaboration between a human and AI. The point is not that everything should become an agent. The point is that there's a real design space between raw chat and full automation, and a lot of the most interesting work is happening right there.`;
  }

  static closing2() {
    return `If you're building in that space too, I'd love to hear from you. And if you're at one of the companies drawing lines around what counts as a legitimate tool, I hope you leave room for the people making this middle ground more useful, more teachable, and more alive.`;
  }

  static sections() {
    return [
      {
        id: 'intro',
        paragraphs: [{ html: this.intro1() }, { html: this.intro2() }],
      },
      {
        id: 'middle',
        title: 'The Middle Ground',
        paragraphs: [
          { html: this.middle1() },
          { html: this.middle2() },
          { html: this.middle3() },
        ],
      },
      {
        id: 'why',
        title: 'Why This Matters',
        paragraphs: [
          { html: this.why1() },
          { html: this.why2() },
          { html: this.why3() },
        ],
      },
      {
        id: 'lines',
        title: 'Where the Lines Are Being Drawn',
        paragraphs: [
          { html: this.lines1() },
          { html: this.lines2() },
          { html: this.lines3() },
          { html: this.lines4() },
        ],
      },
      {
        id: 'recursi',
        title: 'What Recursi Is',
        paragraphs: [
          { html: this.recursi1(), sidebar: this.sidebarSelfImproving() },
          { html: this.recursi2() },
          { html: this.recursi3(), sidebar: this.sidebarMarkdownNotebook() },
          { html: this.recursi4() },
          { html: this.recursi5(), sidebar: this.sidebarAardvark() },
        ],
      },
      {
        id: 'bigger',
        title: 'The Bigger Point',
        paragraphs: [
          { html: this.bigger1() },
          { html: this.bigger2() },
          { html: this.bigger3() },
        ],
      },
      {
        id: 'closing',
        paragraphs: [{ html: this.closing1() }, { html: this.closing2() }],
      },
    ];
  }

  static sidebarSelfImproving() {
    return {
      title: 'Recursive, but not hand-wavy',
      body: `The name is literal. Vibes and YOLO started helping build and improve each other, and the whole system crossed a line where "self-improving" stopped sounding like branding and started sounding descriptive.`,
    };
  }

  static sidebarMarkdownNotebook() {
    return {
      title: 'Filling a real gap',
      body: `Chatbot web UIs are good at generating text and bad at helping you keep it alive. Markdown Notebook exists because "copy this somewhere else and hope for the best" is not a satisfying workflow.`,
    };
  }

  static sidebarAardvark() {
    return {
      title: 'Preview, not vapor',
      body: `Aardvark already has real pieces live, and more is coming. The piano system in particular is substantial enough that it could have been its own product. Instead it got folded into the larger environment.`,
    };
  }

}

